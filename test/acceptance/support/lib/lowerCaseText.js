/**
 * @name lowerCaseText
 * @description makes string lower
 * @param {string} str
 * @returns {string}
 */
export const lowerCaseText = (str) => str.toLowerCase().replace(/\s/g, '_')

export default lowerCaseText
